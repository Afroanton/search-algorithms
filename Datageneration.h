#pragma once
#include <vector>

/// @brief generates an bool array where the index is set to true if that nummber is a prime all the way upp to the upper bound 'max'.
/// @param max 'max' is the upper bound.
/// @return a poniter to the array
bool* find_primes(int max);